#include <stdio.h>

int main(int argc, char const *argv[])
{
    int a,b,c,multiply;
    printf("enter three no \n");
    scanf("%d,%D,%d",&a,&b,&c);
    multiply=a*b*c;
    printf("multiplication=%d",multiply);
    
    return 0;
}
